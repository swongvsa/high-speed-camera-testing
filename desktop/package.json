{
  "name": "high-speed-camera-desktop",
  "private": true,
  "scripts": {
    "start":        "electrobun run",
    "dev":          "electrobun dev",
    "build:dev":    "bun install && electrobun build",
    "build:stable": "electrobun build --env=stable"
  },
  "dependencies": {
    "electrobun": "^1.0.0"
  }
}
