# ==========================================
# High-Speed Camera App - Environment Configuration
# ==========================================
# Copy this file to .env and customize for your deployment
# cp .env.example .env
# ==========================================

# ------------------------------------------
# Camera Configuration
# ------------------------------------------
# IP address of your GigE MindVision camera
# Use a link-local address (169.254.x.x) if camera is directly connected
# Or your network's assigned IP if on corporate network
CAMERA_IP=169.254.22.149

# Camera port (if applicable, usually not needed for GigE)
CAMERA_PORT=8080

# ------------------------------------------
# Gradio Web UI Configuration
# ------------------------------------------
# Port for the web interface (default: 7860)
GRADIO_PORT=7860

# Server bind address (leave as 0.0.0.0 for Docker)
GRADIO_SERVER_NAME=0.0.0.0

# ------------------------------------------
# Docker Compose Profile
# ------------------------------------------
# Use one of: development, production, webcam
#   development  - Build from local source code
#   production   - Use pre-built image from Docker Hub
#   webcam       - Test mode with webcam (no MindVision camera required)
COMPOSE_PROFILES=development

# ------------------------------------------
# Optional: Docker Registry
# ------------------------------------------
# Override the Docker Hub organization name
# Default: highspeedcamera
# DOCKER_REGISTRY=highspeedcamera
